#ifndef MIN_PART_HPP
#define MIN_PART_HPP
#include "Cost.h"   
//A function that returns the minimum of the cost volume, or anything similarly shaped, along the layers direction. Used for initialization and updating A from the Eaux.
// template <class T, class PixelType>
// extern void Cost<T,PixelType>::min(T*,cv::Mat& minIndex,cv::Mat& minValue);

// template
// void Cost<float,cv::Vec<float,3>>::min(T*,cv::Mat& minIndex,cv::Mat& minValue);
#endif
